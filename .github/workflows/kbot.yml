name: CI

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v2
    - name: Configure and Build
      run: cmake -Wno-dev --build .
    - name: Tests
      run: ctest -VV
